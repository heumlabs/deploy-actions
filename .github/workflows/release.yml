on:
  push:
    branches:
      - release

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: deploy action
        uses: heumlabs/deploy-actions@v10
        with:
          repo: heumlabs/deploy-actions
          branch: release
          title: r2m
